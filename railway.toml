[build]
builder = "nixpacks"
buildCommand = "npm run build"
watchPatterns = ["studio/**"]

[deploy]
startCommand = "npm run start"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
